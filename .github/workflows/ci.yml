# Purpose:
#   Run tests
#   Validate builds
#   Validate PostgreSQL integration
#   Ensure application compiles correctly

name: CI

on:
  pull_request:
      branches:
      - main
      - dev
  push:
    branches:
      - main
      - dev

permissions:
  contents: read      

jobs:
  test:
    name: Test & Build Application
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres # gitguardian: ignore
          POSTGRES_PASSWORD: E02postgresql # gitguardian: ignore
          POSTGRES_DB: app_test # gitguardian: ignore
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DB_HOST: ${{ secrets.TEST_DB_HOST }} # gitguardian: ignore
      DB_PORT: ${{ secrets.TEST_DB_PORT }} # gitguardian: ignore
      DB_USER: ${{ secrets.TEST_DB_USER }} # gitguardian: ignore
      DB_PASSWORD: ${{ secrets.TEST_DB_PASSWORD }} # gitguardian: ignore
      DB_NAME: ${{ secrets.TEST_DB_NAME }} # gitguardian: ignore
      DB_SSLMODE: ${{ secrets.TEST_DB_SSLMODE }} # gitguardian: ignore

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'
          check-latest: true
          cache: true

      # - name: Clean Go caches
      #   run: |
      #     go clean -cache
      #     go clean -modcache
      #     rm -rf cache/go-build

      - name: Download dependencies
        run: go mod download

      - name: Run vet
        run: go vet ./...

      - name: Run tests with coverage
        run: |
          mkdir -p coverage

          go test ./... \
          -v \
          -race \
            -covermode=atomic \
            -coverprofile=coverage/coverage.out

      - name: Generate coverage summary
        run: |
          go tool cover -func=coverage/coverage.out

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/coverage.out

      - name: Build API
        run: |
          mkdir -p bin
          go build -o bin/api ./cmd/api/