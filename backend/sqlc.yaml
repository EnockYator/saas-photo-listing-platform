# SQLC Configuration File
  
  ##########################################
  # # Users Domain
  ##########################################
  
  #  Users table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/*users_table.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/users.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/users/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Listings domain
  ##########################################

  # Listing table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/listings/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/listings.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/listings/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Files domain
  ##########################################

  # Files table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/files/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/files.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/files/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Payments domain
  ##########################################

  # Payments table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/payments/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/payments.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/payments/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Notifications domain
  ##########################################

  # Notifications table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/notifications/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/notifications.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/notifications/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ########################################## 
  # Subscriptions domain
  ##########################################

  # Subscriptions table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/subscriptions/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/subscriptions.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/subscriptions/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  # Plans table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/*plans_table.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/plans.sql"
      gen:
        go: 
          package: "sqlc"
          out: "internal/domains/subscriptions/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true 

  # Plan_limits table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/plan_limits/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/plan_limits.sql"
      gen:
        go: 
          package: "sqlc"
          out: "internal/domains/subscriptions/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Audit domain
  ##########################################

  # Audit_logs table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/audit_logs/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/audit_logs.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/audit_logs/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Sharing domain
  ##########################################

  # Share_links table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/share_links/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/share_links.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/sharing_links/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Usage_Stats domain
  ##########################################

  ## Usage_stats table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/usage_stats/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/usage_stats.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/usage_stats/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  ##########################################
  # # Tenants domain
  ##########################################

  # Tenants table
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/*tenants_table.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/tenants.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/tenants/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  # tenant_storage_usage
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/tenant_storage_usage/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/tenant_storage_usage.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/tenants/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

  # Tenant_users
  version: "2"
  sql:
    - engine: "postgresql"
      schema: "internal/infrastructure/database/postgres/migrations/tenant_users/*.up.sql"
      queries: "internal/infrastructure/database/postgres/queries/tenant_users.sql"
      gen:
        go:
          package: "sqlc"
          out: "internal/domains/tenants/infrastructure/repository/sqlc"
          emit_json_tags: true
          emit_prepared_queries: true

